```sh
# install skel, a template manager
npm install -g @yankeeinlondon/skel
# run skel from command line
skel tapp
# or run interactive menu and find "tapp" which is installed 
# by default with skel
skel
```
